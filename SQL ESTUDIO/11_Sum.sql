 Select SUM(age)
From users;

// Se usa para sumar la columna seleccionada, por ejemplo la edad (age) si usamos el 
Sum(age) entonces nos daria como resultado la suma de todas las edades registradas //